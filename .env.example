# Origin of the product app (auth, dashboard) that this marketing site links to.
# In production, e.g. https://app.propello.io — leave blank for local preview.
NEXT_PUBLIC_APP_URL=

# Optional: endpoint the contact form POSTs to. If unset, the form validates and
# shows success without sending (useful for preview).
NEXT_PUBLIC_CONTACT_API=
